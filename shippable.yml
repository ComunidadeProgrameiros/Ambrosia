language: c
compiler:
 - gcc

build:
 ci:
  - sudo add-apt-repository ppa:vala-team
  - sudo apt-get update
  - sudo apt-get install cmake valac -y
  - which valac
  - valac --version
  - mkdir build && cd build
  - cmake ..
  - make
