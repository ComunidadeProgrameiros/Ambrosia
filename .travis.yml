os: linux
language: c
sudo: false
compiler: gcc
addons:
  apt:
   packages:
    - cmake
    - valac
    - libgtk-3-dev
    - gnome-common
    - libglib2.0-dev
    - libsqlite3-dev
    - gobject-introspection
script:
 - mkdir build && cd build
 - cmake ..
 - make
